import BrowseInput from "./BrowseInput";
import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";

<Meta title="Inputs/BrowseInput" component={BrowseInput} />

**Note:** `<BrowseInput />` relies on Electron's `dialog` API. This component is only here to show how it'll look.

<Canvas>
	<Story name="BrowseInput">
       <BrowseInput />
	</Story>
</Canvas>
