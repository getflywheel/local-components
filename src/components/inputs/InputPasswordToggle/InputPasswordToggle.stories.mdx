import InputPasswordToggle from "./InputPasswordToggle";
import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";

<Meta title="Inputs/InputPasswordToggle" component={InputPasswordToggle} />

<Canvas>
	<Story name="InputPasswordToggle">
        <InputPasswordToggle onChange={(event) => console.log('onChange: ', event)} />
    </Story>
</Canvas>

<Canvas>
	<Story name="InputPasswordToggle Invalid">
        <InputPasswordToggle invalid invalidMessage="Input Invalid" onChange={(event) => console.log('onChange: ', event)} />
    </Story>
</Canvas>
