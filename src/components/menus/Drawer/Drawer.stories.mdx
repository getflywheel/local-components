import Drawer from './Drawer';
import { DrawerExample, DrawerExample2 } from './examples/DrawerExample';

import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";

<Meta title="Menus/Drawer" component={Drawer} />

The Drawer component, which is hidden by default, contains content that can be programmatically shown or hidden as needed.
This content slides up and down from below the container its used within.

<Canvas>
	<Story name="Example Drawer">
        <div style={{height: '200px'}}>
            <DrawerExample />
        </div>
	</Story>
</Canvas>

# Example 2

<Canvas>
	<Story name="Example Drawer 2">
        <div style={{height: '200px'}}>
            <DrawerExample2 />
        </div>
	</Story>
</Canvas>
