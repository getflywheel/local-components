Basic:

```js
<Popup 
	position="top"
	triggerContent={<h1>Click to open</h1>}
>
    <span>hello hello hello hello</span>
</Popup>
```

```js
<Popup 
	position="right"
	triggerContent={<Button>Open Dropdown (but up)</Button>}
>
    <h1>lala</h1>
</Popup>
```

Open above:

```js
<Popup 
	position="bottom"
	triggerContent={<Button className="__Pill __Green">Open Dropdown (but up)</Button>}
>
    <img src="https://getflywheel.com/wp-content/uploads/2017/06/php-7-small.png" style={{width: "100%"}} />
</Popup>
```
